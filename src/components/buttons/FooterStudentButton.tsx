import Button from "@mui/material/Button";
/**
 * functional component for a custom button for footer
 * **/
export const FooterStudentButton = () => {
    return(<Button variant={"outlined"}>als PDF exportieren</Button>)
}